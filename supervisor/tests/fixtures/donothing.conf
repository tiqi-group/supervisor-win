[supervisord]
logfile=%(ENV_TMP)s\\donothing.log ; (main log file;default $CWD/supervisord.log)
pidfile=%(ENV_TMP)s\\donothing.pid ; (supervisord pidfile;default supervisord.pid)
nodaemon=true              ; (start in foreground if true;default false)

